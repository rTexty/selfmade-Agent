name: Auto approve

on:
  pull_request_target:
    types: [opened, reopened]

jobs:
  approve:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
    steps:
      - uses: hmarr/auto-approve-action@v4
